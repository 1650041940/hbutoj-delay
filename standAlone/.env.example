# 由本文件生成本地配置：cp -n standAlone/.env.example standAlone/.env
# 注意：standAlone/.env 不应提交到 git（包含密码/密钥）。

# HBUTOJ 全部数据存储的文件夹位置（默认当前路径生成 hbutoj 文件夹）
HBUTOJ_DATA_DIRECTORY=./hbutoj

# ===== 镜像发布/拉取配置（用于“改源码 -> 发镜像 -> 部署 pull 更新”） =====
# 默认指向上游镜像命名空间；如需使用你自己的镜像仓库，请替换为你的 GitHub 用户名或组织名。
HBUTOJ_IMAGE_PREFIX=ghcr.io/1650041940
HBUTOJ_MYSQL_IMAGE_PREFIX=ghcr.io/1650041940
HBUTOJ_MYSQL_CHECKER_IMAGE_PREFIX=ghcr.io/1650041940

# 可选：直接指定完整 MySQL 镜像名（优先级最高）。
# - 适用于你不想维护/推送自定义 DB 镜像时（比如直接用官方 mysql）。
# - 例：HBUTOJ_MYSQL_IMAGE_FULL=mysql:8.0.43
# 留空则走下面的 prefix/name/tag 拼接逻辑。
HBUTOJ_MYSQL_IMAGE_FULL=

HBUTOJ_IMAGE_TAG=v1.0.0
HBUTOJ_BACKEND_IMAGE_TAG=v1.0.0
HBUTOJ_FRONTEND_IMAGE_TAG=v1.0.0
HBUTOJ_JUDGESERVER_IMAGE_TAG=v1.0.0
HBUTOJ_MYSQL_IMAGE_TAG=latest
HBUTOJ_MYSQL_CHECKER_IMAGE_TAG=latest
HBUTOJ_MYSQL_IMAGE=hbutoj_database
HBUTOJ_BACKEND_IMAGE=hbutoj_backend
HBUTOJ_FRONTEND_IMAGE=hbutoj_frontend
HBUTOJ_JUDGESERVER_IMAGE=hbutoj_judgeserver
HBUTOJ_MYSQL_CHECKER_IMAGE=hbutoj_database_checker

# redis的配置
REDIS_HOST=172.20.0.2
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME

# mysql的docker内网ip
MYSQL_HOST=172.20.0.3
MYSQL_PUBLIC_PORT=3306
MYSQL_ROOT_PASSWORD=CHANGE_ME
# 如果判题服务是分布式，请提供当前mysql所在服务器的公网ip
MYSQL_PUBLIC_HOST=127.0.0.1

# nacos的配置
NACOS_HOST=172.20.0.4
NACOS_PORT=8848
NACOS_USERNAME=root
NACOS_PASSWORD=CHANGE_ME

# backend后端服务的配置
BACKEND_HOST=172.20.0.5
BACKEND_PORT=6688
JWT_TOKEN_SECRET=default
JWT_TOKEN_EXPIRE=86400
JWT_TOKEN_FRESH_EXPIRE=43200
JUDGE_TOKEN=default
EMAIL_SERVER_HOST=smtp.qq.com
EMAIL_SERVER_PORT=465
EMAIL_USERNAME=your_email_username
EMAIL_PASSWORD=your_email_password

HDU_ACCOUNT_USERNAME_LIST=
HDU_ACCOUNT_PASSWORD_LIST=
CF_ACCOUNT_USERNAME_LIST=
CF_ACCOUNT_PASSWORD_LIST=
POJ_ACCOUNT_USERNAME_LIST=
POJ_ACCOUNT_PASSWORD_LIST=
ATCODER_ACCOUNT_USERNAME_LIST=
ATCODER_ACCOUNT_PASSWORD_LIST=
SPOJ_ACCOUNT_USERNAME_LIST=
SPOJ_ACCOUNT_PASSWORD_LIST=
LIBRE_ACCOUNT_USERNAME_LIST=
LIBRE_ACCOUNT_PASSWORD_LIST=
FORCED_UPDATE_REMOTE_JUDGE_ACCOUNT=false

# judgeserver的配置
JUDGE_SERVER_IP=172.20.0.7
JUDGE_SERVER_PORT=8088
JUDGE_SERVER_NAME=judger-alone
MAX_TASK_NUM=-1
REMOTE_JUDGE_OPEN=true
REMOTE_JUDGE_MAX_TASK_NUM=-1
PARALLEL_TASK=default

# docker network的配置
SUBNET=172.20.0.0/16

# ===== 内存与 JVM 限制（保证整体内存占用可控，默认总和 < 3.9G） =====
REDIS_MEM_LIMIT=64M

MYSQL_MEM_LIMIT=512M
MYSQL_INNODB_BUFFER_POOL_SIZE=256M
MYSQL_MAX_CONNECTIONS=200

NACOS_MEM_LIMIT=256M
NACOS_JVM_XMX=192m
NACOS_JVM_XMS=192m
NACOS_JVM_XMN=96m

BACKEND_MEM_LIMIT=448M
BACKEND_JAVA_OPTS="-Xms128m -Xmx288m"

FRONTEND_MEM_LIMIT=80M
FRONTEND_HOST=172.20.0.6

JUDGESERVER_MEM_LIMIT=448M
JUDGESERVER_JAVA_OPTS="-Xms128m -Xmx288m"

MYSQL_CHECKER_MEM_LIMIT=96M
MYSQL_CHECKER_HOST=172.20.0.8
AUTOHEAL_MEM_LIMIT=24M
